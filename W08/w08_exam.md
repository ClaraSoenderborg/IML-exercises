# Q6 - filtering

### a: filtering, convolution, correlation
02-filterbasics

filter = sum of elementwise multiplication
filter applied to chunk of image, slide 

blurring image with gaussian filter, weighted average 

blurring 1d signal, 01-data cleaning

correlation = filter applied as-is
convolution = filter is flipped before applying

### b: derivatives, gradients, edges
02-filter

find strong changes in data = edges

image = multivariate function, needs partial derivative for each parameter
gradient, all partial derivatives, points to largest slope 

as filter, sobel filter, finds differences in directions

1d filter applied for derivative of gaze signal, 01-data-cleaning
