# Q5 - model complexity

### a: linear least squares problem
01-polynomial regression

scatter plot, which model would fit best? Inductive bias
try different order polynomials, use LLS to find best fit
design matrix, use kernel as row. Kernel transform to higher dimension, depending on order.
express non-linear function as inner products
fit line with LLS, pseudo inverse. Evaluate on smallest error

### b: data quality, uncertainty, overfitting, underfitting
01-polynomial regression

scatter plot, which model fits best? inductive bias 
data imbalance, data quality
generalisation (error) - bias and variance
training error / testing error
underfitting, too simple, overfitting too complex 

### c: Model ability to generalise to unseen data. Data evaluation, train-test split, cross-validation and performance metrics
02-model_complexity, bottom

generalisation error, ability to generalise. Bias and variance  

train-test split importance
what data is chosen?
k-fold cross validation, train multiple times on different data and summarize



